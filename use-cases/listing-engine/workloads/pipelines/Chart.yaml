apiVersion: v2
name: listing-engine-pipelines
description: >-
  Continuous-training pipeline of the Listing Engine on Argo Workflows: the
  ctsteps contract as a WorkflowTemplate, a weekly safety-net schedule and the
  drift monitor that retrains on demand.
type: application
version: 0.1.0
