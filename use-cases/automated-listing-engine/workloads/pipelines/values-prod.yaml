# prod profile — ILLUSTRATIVE. Same WorkflowTemplate; more data, a PR instead
# of a direct push, and the event-driven trigger from the data platform
# (Airflow sensor / Kafka) documented in docs/modules/orchestration.md.
data:
  rows: 2000000
promote:
  valuesFiles:
    - use-cases/automated-listing-engine/workloads/serving/values-prod.yaml
    - use-cases/automated-listing-engine/workloads/api/values-prod.yaml
resources:
  requests: {cpu: "4", memory: 16Gi}
  limits: {cpu: "8", memory: 32Gi}
