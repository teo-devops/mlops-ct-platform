ingress:
  enabled: true
# Served versions (lineage). Bumped by the pipeline together with
# ../serving/values-demo.yaml — see ctsteps promote.
models:
  categorizer:
    version: "2"
  fraud:
    version: "1"
# opt-in event-bus module on (docs/modules/event-bus.md)
eventBus:
  enabled: true
