ingress:
  enabled: true
# Served versions (lineage). Bumped by the pipeline together with
# ../serving/values-demo.yaml — see ctsteps promote.
models:
  categorizer:
    version: "1"
  fraud:
    version: "2"
# opt-in progressive delivery on (docs/modules/progressive-delivery.md)
rollout:
  enabled: true
