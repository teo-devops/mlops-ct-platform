apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The Listing Engine use case (charts under use-cases/listing-engine/workloads/).
resources:
  - listing-engine-api.yaml
  - listing-engine-pipelines.yaml
  - listing-engine-serving.yaml
